// Create array
var todos = ['item1', 'item2', 'item3'];

// Display array
console.log(todos);

// Add item to todos list
todos.push('new item');
console.log(todos);

//Update array
todos[0] = 'item1 update';
console.log(todos)

// Delete array item
todos.splice(1,1);
console.log(todos);
